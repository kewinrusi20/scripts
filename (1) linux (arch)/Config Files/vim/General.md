# Set up <ins>default</ins> file editor:

inside `.bashrc`
```bash
# add this lines
#export VISUAL=vim # for vim
export VISUAL=nvim # for neovim
export EDITOR="$VISUAL"
```

confirm by writing in console: `source ~/.bashrc`

---
