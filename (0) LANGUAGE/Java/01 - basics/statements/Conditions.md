# Java

<span style="color:red">Note:</span> 

```java
String result = (condition1) ? "won" :
                (condition2) ? "won" :
                (condition3) ? "won" :
                "";

System.out.println(result);

```